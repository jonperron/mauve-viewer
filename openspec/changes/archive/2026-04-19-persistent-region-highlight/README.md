# persistent-region-highlight

Region selection via Shift+drag now creates a persistent highlight instead of auto-zooming. Shift+click clears selection. Matches Java Mauve RangeHighlightPanel behavior.
