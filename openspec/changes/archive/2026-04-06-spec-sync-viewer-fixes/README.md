# spec-sync-viewer-fixes

Sync specs with implemented viewer fixes: Escape to clear region selection, options panel action buttons (Export Image/Print), cursor overlay wheel forwarding for Ctrl+scroll zoom, and pan clamping to prevent SVG overflow
